setwd("c:/users/jim/documents/dataanalysis")
dataset=read.csv("whatsapp_fullfix.csv", sep="~", header=TRUE)
dataset
library("ggplot2", lib.loc="~/R/win-library/3.3")
install.packages("ggplot2")
library(ggplot2)
weekday.f <-factor(dataset$WEEKDAY)
weekday.f
name.f <-factor(dataset$NAME)
name.f
dataset$NAME <-as.factor(dataset$NAME)
dataset$NAME
dataset
dataset$name <-as.factor(dataset$name)
dataset$name
str(dataset$name)
library(sm)
attach(dataset)
library(sm)
install.packages("sm")
library(sm)
attach(dataset)
name.f <- factor(dataset$name, levels= c(1,2,3,4),
labels = c("Alex", "Dave", "Jim", "Sean"))
# plot densities
sm.density.compare(dataset$HOUR, name.f, xlab="Hour of Day")
title(main="Texts by Hour of Day")
# add legend via mouse click
colfill<-c(2:(2+length(levels(cyl.f))))
legend(locator(1), levels(cyl.f), fill=colfill)
# plot densities
sm.density.compare(dataset$HOUR, name.f, xlab="Hour of Day")
title(main="Texts by Hour of Day")
# add legend via mouse click
colfill<-c(2:(2+length(levels(name.f))))
legend(locator(1), levels(name.f), fill=colfill)
plot()
plot.new
# plot densities
sm.density.compare(dataset$HOUR, name.f, xlab="Hour of Day")
title(main="Texts by Hour of Day")
# add legend via mouse click
colfill<-c(2:(2+length(levels(name.f))))
legend(locator(1), levels(name.f), fill=colfill)
# plot densities
plot(sm.density.compare(dataset$HOUR, name.f, xlab="Hour of Day"))
title(main="Texts by Hour of Day")
# add legend via mouse click
colfill<-c(2:(2+length(levels(name.f))))
legend(locator(1), levels(name.f), fill=colfill)
# plot densities
plot.new(sm.density.compare(dataset$HOUR, name.f, xlab="Hour of Day"))
title(main="Texts by Hour of Day")
# add legend via mouse click
colfill<-c(2:(2+length(levels(name.f))))
legend(locator(1), levels(name.f), fill=colfill)
sm.density.compare(x=dataset$HOUR, y=name.f, xlab="Hour of Day")
sm.density.compare(dataset$HOUR, name.f, xlab="Hour of Day")
name.f
name.f <- factor(dataset$name, levels= c(1,2,3,4),
labels = c("Alex", "Dave", "Jim", "Sean"))
name.f
sm.density.compare(dataset$HOUR, dataset$name, xlab="Hour of Day")
title(main="Texts by Hour of Day")
# add legend via mouse click
colfill<-c(2:(2+length(levels(name.f))))
legend(locator(1), levels(name.f), fill=colfill)
sm.density.compare(dataset$HOUR, dataset$name, xlab="Hour of Day")
title(main="Texts by Hour of Day")
# add legend via mouse click
colfill<-c(2:(2+length(levels(name.f))))
legend(locator(1), levels(name.f), fill=colfill)
# plot densities
sm.density.compare(dataset$WEEKDAY, dataset$name, xlab="Hour of Day")
title(main="Texts by Hour of Day")
# add legend via mouse click
colfill<-c(2:(2+length(levels(name.f))))
legend(locator(1), levels(name.f), fill=colfill)
# plot densities
sm.density.compare(dataset$HOUR, dataset$name, xlab="Hour of Day")
title(main="Texts by Hour of Day")
# add legend via mouse click
colfill<-c(2:(2+length(levels(dataset$name))))
legend(locator(1), levels(dataset$name), fill=colfill)
# plot densities
sm.density.compare(dataset$WEEKDAY, dataset$name, xlab="Day of Week")
title(main="Texts by Day of Week")
xlim=c(1,7)
# add legend via mouse click
colfill<-c(2:(2+length(levels(dataset$name))))
legend(locator(1), levels(dataset$name), fill=colfill)
# plot densities
sm.density.compare(dataset$WEEKDAY, dataset$name, xlab="Day of Week" xlim=c(1,7))
title(main="Texts by Day of Week")
# add legend via mouse click
colfill<-c(2:(2+length(levels(dataset$name))))
legend(locator(1), levels(dataset$name), fill=colfill
)
# plot densities
sm.density.compare(dataset$WEEKDAY, dataset$name, xlab="Day of Week" xlim=c(1,7))
title(main="Texts by Day of Week")
# add legend via mouse click
colfill<-c(2:(2+length(levels(dataset$name))))
legend(locator(1), levels(dataset$name), fill=colfill)
# plot densities
sm.density.compare(dataset$WEEKDAY, dataset$name, xlab="Day of Week", xlim=c(1,7))
title(main="Texts by Day of Week")
# add legend via mouse click
colfill<-c(2:(2+length(levels(dataset$name))))
legend(locator(1), levels(dataset$name), fill=colfill)
# plot densities
sm.density.compare(dataset$HOUR, dataset$name, xlab="Hour of Day", xlim=c(1,7))
title(main="Texts by Hour of Day")
# add legend via mouse click
colfill<-c(2:(2+length(levels(dataset$name))))
legend(locator(1), levels(dataset$name), fill=colfill)
# plot densities
sm.density.compare(dataset$HOUR, dataset$name, xlab="Hour of Day", xlim=c(1,24))
title(main="Texts by Hour of Day")
# add legend via mouse click
colfill<-c(2:(2+length(levels(dataset$name))))
legend(locator(1), levels(dataset$name), fill=colfill)
